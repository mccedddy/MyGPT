Here are the final instructions based on our findings:

### 1. Confirm Current Status
Your Ollama instance (`ollama serve`) is currently active in PowerShell with **qwen3.5:9b** loaded (or available)
and listening by default. No commands to start or kill are needed right now. The API is already live at the
standard address.

### 2. Accessing the API
Since your server is running on `127.0.0.1` (localhost), you do not need flags like `--host`. Your external tools
should connect directly to these endpoints:

*   **Base URL:** `http://127.0.0.1:11434`
    *   *(Note: If you want your web app on the same machine or another device in your LAN, replace `127.0.0.1`
with your actual IP address like `192.168.x.x`, but for local testing via PowerShell or localhost apps, use this
base).*

#### Verification Endpoint
Before sending full prompts to avoid token limits:
```bash
curl http://localhost:11434/api/tags
# OR in PowerShell if you prefer native calls without curl (if installed) / Invoke-RestMethod:
$models = Invoke-RestMethod -Uri "http://localhost:11434/api/tags"
Write-Host $models.models.name | Out-String
```

#### Generating Text Endpoint
Use this to send your actual prompts. Note that **qwen3.5** models can sometimes require the full model name or a
shorter alias depending on how they were installed, but `api/chat` is standard:

```bash
# Example using PowerShell Invoke-RestMethod (No curl needed)
$body = @{
    model  = "qwen3.5"
    prompt = "What is your main capability?"
    stream = $true | ConvertTo-Json # Set to false if streaming issues occur in older clients
} -as [pscustomobject]

Invoke-RestMethod -Uri "http://localhost:11434/api/chat" -Body ($body | ConvertTo-Json) | Out-String |
Invoke-WebRequest -UseBasicParsing
```

### 3. Important Configuration Notes for Your Setup
*   **Model Name Consistency**: Ensure your application code (Python, Node.js, etc.) references the model exactly
as registered in Ollama's internal list (`qwen3.5:9b`). If you shortened it to just `qwen3.5` or similar, ensure
that short alias resolves correctly via `/api/tags`.
*   **Port Binding**: Your instance is bound at port `11434`. Do not change this unless explicitly configured in
the Ollama config file (which usually resides outside your PowerShell session).
*   **Streaming vs. Batch Mode**: If you encounter errors with large context windows on qwen3.5:9b, try setting
`stream = false` in your request payload to get a single full response instead of line-by-line chunks.

### Summary Command for Your External App
When initializing your connection